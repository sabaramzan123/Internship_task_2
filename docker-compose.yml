services:
  backend:
    build: 
      context: ./backend
      dockerfile: Dockerfile
    container_name: aether_backend
    volumes:
      - ./backend:/app
      - ./data:/app/data
    ports:
      - "8000:8000"
    restart: always